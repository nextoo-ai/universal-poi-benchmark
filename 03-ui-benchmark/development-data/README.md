# Development data

Place the consolidated Step 2 dataset in this directory as:

`canonical-rome.json`

The directory is ignored by Git except for this README. The file is private input
for UI design and testing and must not appear in any submitted application build,
source archive, fixture, screenshot, service worker, cache, or documentation.

The evaluator will inspect submitted artifacts for dataset fingerprints and sampled
POI content before design review.
